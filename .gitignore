# Operating System files
.DS_Store
Thumbs.db
venv
setup.sh
=4.5.0
ngrok.tgz
ngrok

# Node modules
node_modules/

# Log files
*.log


# Secrets (NEVER commit secrets!)
secrets.txt
.env
.env.local
service-account-key.json
backend/data
backend/__pycache__/__init__.cpython-311.pyc
backend/logs
data
.vscode
.next
.idx
.cursor
.firebase
.hermes
# Python cache files
backend/__pycache__/
__pycache__/
*.py[cod]
*$py.class

# Large data files (consider Git LFS if necessary)
*.dat"commit_message.txt" 


colab/r1v0.1.ipynb
gha-creds-*.json

backend/configs/firrebase/service-account-key.json
memory.md
IMPROVEMENTS.md
backend/scripts
# Model weights (6.5MB binaries; re-downloadable from ultralytics)
*.pt
# TensorRT/ONNX artifacts (139MB engine, 12.5MB onnx — built per-GPU on the
# Kaggle box, NEVER commit; a pushed .engine exceeds GitHub's 100MB limit and
# breaks the auto-commit deploy loop)
*.engine
*.onnx

# Audit session test files (untracked working-tree artifacts)
backend/tests/
hosting/__tests__/

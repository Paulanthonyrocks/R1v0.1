# backend/requirements.txt

# PyTorch
torch>=2.0.0
torchvision>=0.15.0

# Core FastAPI
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
python-multipart>=0.0.5
httpx>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Utilities
pyyaml>=6.0
psutil>=5.9.0
tenacity>=8.2.0
watchdog>=2.1.0
bleach>=6.1.0

# ML/CV
ultralytics>=8.0.0
opencv-python-headless==4.8.0.74
scipy>=1.8.0
filterpy>=1.4.5
numpy<2.0
scikit-learn>=1.0.0
joblib>=1.3.2
onnxruntime-gpu>=1.16.0
nvidia-tensorrt>=8.6.1
pycuda>=2022.1
easyocr>=1.7.0

# Route optimization (route_optimizer.py imports networkx at module top;
# reachable from /api/v1/routes via route_optimization_service).
networkx>=3.0

# TensorFlow (with GPU support)
tensorflow>=2.16.1

# Data
pandas>=1.3.0

# Database
sqlalchemy
asyncpg
aiosqlite
cryptography
aiokafka>=0.8.0
msgpack

# Firebase
firebase-admin>=5.0.0

# Redis
redis>=5.0.0

# Async HTTP
aiohttp>=3.8.0

# WebRTC
aiortc>=1.8.0
av>=11.0.0

# NOTE (audit 2026-08-23): `app/ml/segmentation/edgetam.py` imports `transformers` and
# `app/ml/pavement_analysis/reporting/report_generator.py` imports `matplotlib`, but
# neither module is reachable from the live pipeline (edgetam has no importers;
# pavement analysis is an offline subsystem). Install manually if you use them:
#   pip install transformers matplotlib

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

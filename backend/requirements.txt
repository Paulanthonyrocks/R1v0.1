# backend/requirements.txt
# Core FastAPI
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
python-multipart>=0.0.5
httpx>=0.25.0 # For async HTTP calls
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Configuration & Utilities
pyyaml>=6.0
psutil>=5.9.0
numpy<2.0
tenacity>=8.2.0
watchdog>=2.1.0 # For file system monitoring


# ML/CV Dependencies
# NOTE:  These torch versions might require specific CUDA versions. Adapt as needed for your hardware
# For CPU-only or specific CUDA versions, refer to https://pytorch.org/get-started/locally/
#  and adjust as needed (including the --index-url).  No index URL assumes standard PyPI.
torch>=2.0.0

ultralytics>=8.0.0
opencv-python-headless==4.8.0.74
scipy>=1.8.0
filterpy>=1.4.5
tensorflow>=2.16.1  # Deep learning framework
#tensorflow-cpu>=2.16.1  # CPU-only version for systems without GPU
ml-dtypes>=0.5.0
scikit-learn>=1.0.0  # For StandardScaler and other ML utilities
onnxruntime-gpu>=1.15.1 # For GPU-accelerated inference
onnxruntime>=1.15.1 # Fallback
#transformers>=4.36.0 # For EdgeTAM model

# Pavement Analysis Dependencies

pandas>=1.3.0
torchvision>=0.15.0  # If not already included

# OCR Dependencies
pillow>=9.0.0
pytesseract>=0.3.10
google-genai>=0.1.0
easyocr>=1.7.0

# Database
sqlalchemy
asyncpg
psycopg2-binary
aiosqlite

pymongo>=4.0.0 # For MongoDB support
aiokafka>=0.8.0 # For async Kafka messaging support

# Firebase Admin
firebase-admin>=5.0.0

# Redis & Caching
redis>=5.0.0

# Additional Dependencies
aiohttp>=3.8.0  # For async HTTP requests
pytest>=7.4.0
pytest-asyncio>=0.23.0
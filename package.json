{
  "name": "r1v0-1-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix hosting run dev",
    "build": "npm --prefix hosting run build",
    "start": "npm --prefix hosting run start",
    "lint": "npm --prefix hosting run lint",
    "test": "npm --prefix hosting run test",
    "backend:dev": "cd backend && export PYTHONDONTWRITEBYTECODE=1 && venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app"
  },
  "dependencies": {
    "chrome-devtools-mcp": "^0.19.0"
  }
}
